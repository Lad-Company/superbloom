# Memory: exclude ephemeral logs and working state, keep semantic knowledge tracked
.agent/memory/episodic/
.agent/memory/working/

# OS
.DS_Store

# Environment (should not exist per conventions, but just in case)
.env
.env.*
